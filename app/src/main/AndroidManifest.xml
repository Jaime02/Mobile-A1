<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:name=".App"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MobileA1"
        tools:targetApi="31">
        <activity
            android:name=".ui.activities.MainActivity"
            android:exported="true"
            android:label="@string/activity_main_title"
            android:theme="@style/Theme.MobileA1">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.activities.OrderActivity"
            android:exported="true"
            android:label="@string/order_activity_title"
            android:theme="@style/Theme.MobileA1" />
        <activity
            android:name=".ui.activities.CreateOrderActivity"
            android:exported="true"
            android:label="@string/create_order_activity_title"
            android:theme="@style/Theme.MobileA1" />
    </application>

</manifest>